import React from 'react';

const Home = () => {
    return (
        <div>
            <div>Home component</div>
            <button onClick={() => console.log('hi i am')}>Press me</button>
        </div>
    );
};

export default Home;